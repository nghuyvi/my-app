import { BtnEditDirective } from './btn-edit.directive';

describe('BtnEditDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnEditDirective();
    expect(directive).toBeTruthy();
  });
});
